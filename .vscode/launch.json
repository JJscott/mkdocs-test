{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run MkDocs Server",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/.venv/Scripts/mkdocs.exe",
            "args": ["serve"],
            "console": "integratedTerminal"
        }
    ]
}